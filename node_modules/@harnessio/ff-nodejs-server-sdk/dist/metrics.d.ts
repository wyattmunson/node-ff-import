import * as events from 'events';
import { Configuration, FeatureConfig, Variation } from './openapi';
import { Options, Target } from './types';
export declare enum MetricEvent {
    READY = "metrics_ready",
    ERROR = "metrics_error"
}
export interface MetricsProcessorInterface {
    start(): void;
    close(): void;
    enqueue(target: Target, featureConfig: FeatureConfig, variation: Variation): void;
}
export declare const MetricsProcessor: (environment: string, cluster: string, conf: Configuration, options: Options, eventBus: events.EventEmitter) => MetricsProcessorInterface;
