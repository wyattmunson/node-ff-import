/// <reference types="node" />
import { ClientApi } from './openapi';
import { Options } from './types';
import EventEmitter from 'events';
import { Repository } from './repository';
export declare enum PollerEvent {
    READY = "poller_ready",
    ERROR = "poller_error"
}
export declare class PollingProcessor {
    private environment;
    private cluster;
    private api;
    private stopped;
    private options;
    private repository;
    private initialized;
    private eventBus;
    private timeout;
    private log;
    constructor(environment: string, cluster: string, api: ClientApi, options: Options, eventBus: EventEmitter, repository: Repository);
    private poll;
    private retrieveFlags;
    private retrieveSegments;
    start(): void;
    stop(): void;
    close(): void;
}
