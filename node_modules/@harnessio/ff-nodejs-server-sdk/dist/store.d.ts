import { AsyncKeyValueStore } from './types';
export declare class FileStore implements AsyncKeyValueStore {
    private keyv;
    private keyvFile;
    constructor(options?: {});
    set(key: string, value: unknown): Promise<true>;
    get<T>(key: string): Promise<T>;
    del(key: string): Promise<boolean>;
    keys(): Promise<string[]>;
}
