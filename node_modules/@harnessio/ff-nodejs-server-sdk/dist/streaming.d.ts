/// <reference types="node" />
import EventEmitter from 'events';
import { ClientApi } from './openapi';
import { Options } from './types';
import { Repository } from './repository';
export declare class StreamProcessor {
    static readonly CONNECTED = 1;
    private apiKey;
    private jwtToken;
    private environment;
    private options;
    private cluster;
    private eventBus;
    private api;
    private eventSource;
    private repository;
    private log;
    private isRetrying;
    constructor(api: ClientApi, apiKey: string, environment: string, jwtToken: string, options: Options, cluster: string, eventBus: EventEmitter, repository: Repository);
    start(): void;
    private msgProcessor;
    connected(): boolean;
    stop(): void;
    close(): void;
}
